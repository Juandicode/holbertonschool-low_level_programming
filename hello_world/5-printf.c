#include <stdio.h>

/**
 * main - Prints a piece of art with proper grammar, followed by a new line.
 *
 * Return: Always 0.
 */
int main(void)
{
        printf("\"With proper grammar, but the outcome is a piece of art,\"\n");

        return (0);
}
